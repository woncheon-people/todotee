spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/todotee
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    generate-ddl : false
    hibernate:
      ddl-auto: update


